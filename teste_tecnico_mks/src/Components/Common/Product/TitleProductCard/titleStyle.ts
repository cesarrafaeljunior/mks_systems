import styled from "styled-components";

const fontSiTitleProduct = "1em";

const TitleProductStyled = styled.h2`
  min-height: 40px;
  max-height: 40px;

  color: var(--greyScale3);

  font-size: ${fontSiTitleProduct};
  font-weight: var(--fontWeRegular);

  overflow: hidden;
`;

export { TitleProductStyled };
